---
name: "Coverprofiles"

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - "go.mod"
      - "go.sum"
      - "*.go"
      - "**/*.go"

jobs:
  generate-reports:
    uses: heathcliff26/ci/.github/workflows/golang-testcover-report.yaml@main
    secrets: inherit
    permissions: {}
    with:
      coveralls: true
